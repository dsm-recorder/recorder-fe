import styled from 'styled-components';

export const _NoticeTitle = styled.div`
  color: ${({ theme }) => theme.colors.gray[100]};
  font-size: 24px;
`;

export const _NoticeSub = styled.div`
  color: ${({ theme }) => theme.colors.gray[60]};
  font-size: 24px;
`;
